<div class="container">
<h2><%= @cocktail.name %></h2>

<ul><% @cocktail.doses.each do |dose| %>
  <li><%= dose.ingredient.name %> |
  <%= dose.description %>

  <%= link_to ':corbeille:', dose_path(dose), method: :delete, data: {confirm: 'Are you sure to delete this dose?'} %>
  </li>
<% end %>
</ul>


 <%= link_to 'Add an ingredient', new_cocktail_dose_path(@cocktail) %> | <%= link_to 'Back', cocktails_path %>
